# Copy this file to .env.test and update the username to match your Postgres user.
# This database is wiped between test runs, so keep it separate from your dev database.
DATABASE_URL="postgresql://YOUR_PG_USER@localhost:5432/blogging_platform_test?schema=public"
